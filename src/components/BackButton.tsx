const BackButton = () => {
  return (
    <a
      className="inline-flex items-center gap-2 rounded-full border border-[#1f1b16] px-5 py-2 text-xs font-semibold uppercase tracking-[0.2em] text-[#1f1b16] transition hover:bg-[#1f1b16] hover:text-[#f4f0e8]"
      href="/"
    >
      Back
    </a>
  );
};

export default BackButton;
